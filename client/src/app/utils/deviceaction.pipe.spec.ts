import { DeviceactionPipe } from './deviceaction.pipe';

describe('DeviceactionPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceactionPipe();
    expect(pipe).toBeTruthy();
  });
});
