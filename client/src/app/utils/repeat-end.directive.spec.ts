import { RepeatEndDirective } from './repeat-end.directive';

describe('RepeatEndDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatEndDirective();
    expect(directive).toBeTruthy();
  });
});
